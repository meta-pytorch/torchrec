# Production-like metric scaling benchmark — CPU-offloaded variant.
# Same shape as metric_scaling_prod.yml; uses CPUOffloadedRecMetricModule
# so update() and compute() run on background threads. Pair with
# metric_scaling_prod.yml in the nightly to track end-to-end iter-time
# and qps deltas.
#
# Usage:
#   buck2 run @fbcode//mode/opt fbcode//torchrec/distributed/benchmark:benchmark_train_pipeline -- \
#     --yaml_config=fbcode/torchrec/distributed/benchmark/yaml/metric_scaling_prod_cpu_offload.yml
RunOptions:
  world_size: 2
  num_batches: 10
  num_benchmarks: 3
  num_profiles: 1
  num_iters: 100
  profile_dir: "."
  name: "metric_scaling_prod_cpu_offload"
  loglevel: "info"
PipelineConfig:
  pipeline: "sparse"
ModelInputConfig:
  num_float_features: 100
  feature_pooling_avg: 10
ModelSelectionConfig:
  model_name: "test_sparse_nn"
  model_config:
    num_float_features: 100
    submodule_kwargs:
      dense_arch_out_size: 128
      over_arch_out_size: 1024
      over_arch_hidden_layers: 5
      dense_arch_hidden_sizes: [128, 128, 128]
      skip_regroup: true
EmbeddingTablesConfig:
  num_unweighted_features: 10
  num_weighted_features: 5
  embedding_feature_dim: 32
PlannerConfig:
  pooling_factors: [10.0]
RecMetricConfig:
  enable_metrics: true
  metrics: [ne, calibration, ctr, mse, auc]
  num_tasks: 20
  rec_compute_mode: "unfused"
  compute_interval: 25
  window_size: 10000000
  enable_cpu_offload: true
